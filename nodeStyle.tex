\tikzset{noeudsConditions/.append style = {
        inner sep = 5pt, 
        %outer sep = 0.0cm,
        diamond,
        draw,
        minimum width = 2cm,
        text width = 2cm,
        minimum height = 1.cm,
        line width = 1, 
        %rounded corners,
        align = flush center,
        %on grid,
        %auto,
        aspect = 2,
        anchor = north,
    }
}


\tikzset{noeudsAction/.append style = {
        inner sep = 5pt,
        rectangle,
        draw,
        line width = 1, 
        minimum height = 0.8cm,
        rounded corners,
        align = flush center,
        anchor = north,
    }
}


\tikzset{noeudsEtape/.append style = {
        inner sep = 5pt,
        draw,
        rounded rectangle,
        minimum height = 0.8cm,
        line width = 1,
        align = flush center,
        anchor = north,
    }
}

\tikzset{noeudsDebutFin/.append style = {
        draw, 
        anchor = north,
        line width = 1,
        align = flush center,
        ellipse,
    }
}
